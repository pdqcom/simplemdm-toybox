#!/bin/bash
project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")"/.. && pwd)"

docker build -t "pdq-interview-rails" -f "$project_root/DockerfileRuby" "$project_root/api"
#docker build -t "pdq-interview-rails" -f "$project_root/DockerfileRuby" "$project_root/rails"
